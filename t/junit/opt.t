`dram -j pathname` writes a JUnit report in `pathname`
======================================================

setup::

  $ . $TESTROOT/Fake.dram.bin


test::

  $ dram -j roflmao.wtf t
  DRAM_JUNIT_FILE=roflmao.wtf
